export function AppHeader() {
  return (
    <header className="app-header panel">
      <div>
        <p className="eyebrow">V1 Scope</p>
        <h2>Workflow parity over pixel parity</h2>
      </div>
      <p className="muted">Manual import first, Bloomberg and automation later.</p>
    </header>
  );
}
